<%= form_for(@test_app) do |f| %>
  <% if @test_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_app.errors.count, "error") %> prohibited this test_app from being saved:</h2>

      <ul>
      <% @test_app.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_area :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
